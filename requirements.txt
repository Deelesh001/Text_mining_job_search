flask>=3.0.0
gunicorn>=21.2.0
pandas>=2.1.0
pyarrow>=14.0.0
numpy>=1.24.0
requests>=2.31.0
# fastembed>=0.2.0  # DISABLED: ONNX Runtime loads ~1.5 GB RAM regardless of model size.
                    # Exceeds Cloud Run free tier (512 MB). Dense retriever implemented
                    # in src/retrieval/dense.py but disabled in app.py. Re-enable when
                    # deploying to paid tier with 2-4 GB RAM.
pdfplumber>=0.10.0
python-docx>=1.1.0
beautifulsoup4>=4.12.0
langdetect>=1.0.9
nltk>=3.8.1
argostranslate>=1.10.0